const router = require('express').Router();
const { User } = require('../db');
//const { Candy } = require('../db/index');





module.exports = router


//our app can use to fetch the logged-in user on our session^^
